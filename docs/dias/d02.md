# Dia 2 — Pequenas moléculas

## Objetivos

- Montar um **dataset de pequenas moléculas** (ex.: pares de fragmentos em distâncias
  variadas), usando os exemplos em `D02/data/` (`orca_inp`, `pbds`, `xyz`).
- Executar os cálculos (MOPAC) e analisar a variação de **ΔE ×
  densidade eletrônica intermolecular**.

## Conteúdo

- Dataset com pequenas moléculas, variando a distância.
- Organização nas pastas.
- Execução do cálculo (MOPAC).
- Análise ΔE × compartilhamento.

```{note}
Esta página será expandida conforme o material do dia for finalizado
(arquivo `D02/index.md` no repositório).
```
